
{% extends 'base.html.twig' %}

{% block title %}Edit Measurement{% endblock %}

{% block body %}
    <h1>Edit Measurement</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Save changes</button>
    {{ form_end(form) }}

    <a href="{{ path('measurement_index') }}">back to list</a>
{% endblock %}
